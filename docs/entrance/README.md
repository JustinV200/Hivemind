# Hive Entrance docs

The Landing Board's committed OpenAPI document, a client guide, and the push and device-enrolment
contracts for the Hive Entrance (the Hive's two-listener gateway: loopback, always on, and remote,
only when explicitly exposed). CI fails if the generated OpenAPI document ever drifts from the one
committed here, starting in phase 10.
