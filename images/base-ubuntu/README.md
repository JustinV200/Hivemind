# base-ubuntu image

Terminal-first Virtual Cell image on Ubuntu LTS: a shell, Python, the Warden entry point and the
Waggle client, and nothing else. Every other image builds on this one. Populated with its
Dockerfile or VM spec in phase 5, when the Hive first provisions Virtual Cells.
