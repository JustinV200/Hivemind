# desktop-ubuntu image

Exoskeleton-ready Virtual Cell image: `base-ubuntu` plus Xvfb, xdotool, PulseAudio and a browser,
so a Cell built from this image can offer a virtual display, input and audio when a task attaches
the Exoskeleton. Populated in phase 6, when the Exoskeleton subsystem lands.
