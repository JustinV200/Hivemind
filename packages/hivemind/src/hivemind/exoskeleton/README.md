# hivemind.exoskeleton

The exoskeleton package is the Hive's optional set of peripherals for a Cell: compound_eye
(vision), antennae (input), buzz (audio), a browser attachment, and the tactics a Worker can
invoke, such as the Pheromone Mask's input tactic. Nothing in the core assumes any of this is
attached; a task asks for it explicitly.
