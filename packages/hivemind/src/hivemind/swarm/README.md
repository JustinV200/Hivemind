# hivemind.swarm

The swarm package turns enrolled devices into Real Cells: the swarm registry, enrolment,
heartbeat, PollenSession (a device's CellSession carried over Waggle) and Nuc promotion (giving
a colonized device its own model server so it keeps working disconnected).
