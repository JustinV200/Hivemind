# hivemind.queen.supersedure

The supersedure package is Supersedure: moving the Hive Stand (the machine the Queen runs on) to
another machine by freezing state, copying it across and handing over, with an invariant that
two Queens are never both active.
