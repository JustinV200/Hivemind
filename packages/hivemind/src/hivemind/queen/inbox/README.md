# hivemind.queen.inbox

The inbox package is the Queen's inbox: where questions, Alarms and task events from every
Warden queue up for Attendant (the inbox triage every supervisor uses) before the Queen acts.
