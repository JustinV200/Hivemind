# hivemind.memory

The memory package covers the Hive's hot and warm memory tiers: assembling hot state for an
awake episode, relevance scoring, the Handoff model (a resumable snapshot of a task's memory),
compaction, Pins (memory a task marks as must-keep), Bee Bread (the warm tier) and Cell Wax (the
Queen's written cautions about one Cell).
