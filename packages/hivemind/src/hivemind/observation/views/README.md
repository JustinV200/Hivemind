# hivemind.observation.views

The views package holds one read model per Observation Hive view: the fleet, a single Cell, the
Forage split, Attendant (the inbox triage every supervisor uses) queues, Capping (the pre-effect
safety gate a Proposal must clear) and the Honey browser.
