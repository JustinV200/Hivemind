# hivemind.observation

The observation package is the Observation Hive's read side: pydantic read models (FleetView,
CellView, ForageView and the rest), the API that serves them and metrics. The front end that
renders these views lives separately, in packages/observation-web.
