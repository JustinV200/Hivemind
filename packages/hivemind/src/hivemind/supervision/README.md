# hivemind.supervision

The supervision package defines the Supervisor protocol shared by every level of the Hive's
chain of command (a human, the Queen, a Warden, a sub-bee), plus Alarm, AlarmKind,
ContextTelemetry and the Attendant (the inbox triage every supervisor uses).
