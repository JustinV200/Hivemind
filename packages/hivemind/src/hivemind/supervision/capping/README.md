# hivemind.supervision.capping

The capping package is the Capping gate: a Proposal for a side-effecting action is checked
against a Postcondition and a RiskTier before anything with an effect outside a scratch
directory is allowed to land.
