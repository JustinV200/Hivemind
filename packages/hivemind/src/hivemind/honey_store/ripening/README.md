# hivemind.honey_store.ripening

The ripening package holds the pipeline that turns raw Nectar into Honey: chunking, embedding
and labelling each piece with a HoneyClearance (a data sensitivity tier) before it is allowed
into retrieval.
