# hivemind.honey_store.schema

The schema package holds the SQLite schema and numbered migrations for the Honey Store's tables,
kept separate from ripening and retrieval logic so the on-disk shape can be reviewed on its own.
