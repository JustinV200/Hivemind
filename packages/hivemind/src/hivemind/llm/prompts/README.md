# hivemind.llm.prompts

The prompts package holds the prompt templates the Queen and her bees assemble into an awake
episode (a bounded, stateless turn where a bee is allowed to think with a model). Prompts live
here, not scattered through the callers, so tone and structure stay consistent across the Hive.
