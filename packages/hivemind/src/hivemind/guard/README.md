# hivemind.guard

The guard package is the Hive's policy engine: CapabilitySet (what one bee is currently allowed
to do) and access.py (what each AccessLevel permits). The security tier enums themselves live in
cell/tiers.py; guard only interprets them.
