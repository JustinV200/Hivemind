# observation-web/android

A Capacitor project wrapping the same Observation Hive React app as a native Android APK, with
native push (FCM or UnifiedPush, per the Android ADR) and the credential manager for passkeys
through Capacitor plugins. The APK is a release artifact of phase 12.
