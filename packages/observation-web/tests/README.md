# observation-web/tests

Front-end tests for the Observation Hive, run with `pnpm test` (Vitest) against a jsdom
environment using React Testing Library. `app.test.tsx` is the only test in roadmap step 0.1: it
renders the placeholder app and asserts the Hive's name appears.
